<!-- Neues Unternehmen? Bitte vorher `node discover.mjs` laufen lassen (siehe CONTRIBUTING.md). -->

- [ ] Eintrag in `seed.json` (alphabetisch), Token mit `node discover.mjs` geprüft
- [ ] Keine Änderungen an `README.md`, `jobs.json`, `jobs.csv` oder `lists/` (die erzeugt die Action)
